<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. -->
    <style name="Theme.SparkStudio" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Customize your theme here. -->
    </style>

    <!-- Splash Screen theme. -->
    <style name="Theme.SparkStudio.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/img_splash_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.SparkStudio</item>
    </style>
</resources>
